/** @type {import('tailwindcss').Config} */
export default {
  // Specify the paths to all of our template files
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      // We can add custom colors or themes here later if needed
    },
  },
  plugins: [],
}
